require('ajax.core.settings')
require('ajax.core.mappings')
require('ajax.core.utils')

-- setup keymap at the end
-- local utils = require('ajax.core.utils')
-- local mappings = require('mappings')
-- utils.set_keymaps(mappings)
